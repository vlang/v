# compiled tool
fast

# runtime data / logs
fast.db
fast.db-shm
fast.db-wal
*.log

# oldv helper binary (compiled on demand)
oldv

# benchmark build artifacts
v.c
v2
vprod
fastv3
fast_hello
vwarm_caches
