name: Retire one-off signed integer float integration

on:
  push:
    branches:
      - automation/apply-signed-integer-float-fix-20260921

permissions: {}

concurrency:
  group: apply-signed-integer-float-fix-20260921
  cancel-in-progress: true

jobs:
  retired:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Source integration is handled through Git objects.'
