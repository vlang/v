-module('v.math.bits').
-export([f32_bits/1, f32_from_bits/1, f64_bits/1, f64_from_bits/1]).

f32_bits(F) ->
    P = *todo,
    P.

f32_from_bits(B) ->
    P = *todo,
    P.

f64_bits(F) ->
    P = *todo,
    P.

f64_from_bits(B) ->
    P = *todo,
    P.
