## Description

The `db` module provides a robust and flexible interface for interacting with 
various databases. It has wrappers for PostgreSQL, SQLite, MSSQL and MYSQL 
libraries. See `README.md` each of them for more details. 
