// BEAM sleeping implementation
module os

// sleep_ms provides a cross platform way to sleep
// On BEAM: uses timer:sleep/1 which takes milliseconds
fn sleep_ms(ms i64) {
	// Placeholder - in real impl: timer:sleep(Ms)
}
