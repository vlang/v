import os

fn main() {
	vexe := os.getenv('VEXE')
	vroot_doc := os.dir(vexe) + '/doc'
	mut files := os.ls(vroot_doc) or { panic(err) }
	files.sort()
	for file in files {
		if file.ends_with('.md') {
			println(file)
		}
	}
}
