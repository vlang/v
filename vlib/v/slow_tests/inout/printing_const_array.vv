const (
	dat = 'Data tag ,No data'.split(',')
	dd  = []Info{len: 4, init: Info{if index in tag { dat[0] + index.str() } else { dat[1] }}}
	tag = [1, 2]
)

struct Info {
	val string
}

fn main() {
	println(dd)
}
