#!/usr/bin/env expect
spawn [lindex $argv 0]
set test_file [lindex $argv 1]

expect "${test_file}:10 vdbg> " { send "p b\r" } timeout { exit 1 }
expect "b = foo (&main.Test)" { send "q\r" } timeout { exit 1 }
expect eof
