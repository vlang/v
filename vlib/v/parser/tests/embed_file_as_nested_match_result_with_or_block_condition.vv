fn maybe_int() ?int {
	return none
}

fn yes() bool {
	return true
}

fn main() {
	fallback := $embed_file(@FILE)
	embedded := if yes() {
		match maybe_int() or { 0 } {
			0 { $embed_file(@FILE) }
			else { fallback }
		}
	} else {
		fallback
	}
	assert embedded.len > 0
}
