struct User {
	name string // name
	// last comment
	// last comment2
}

fn main() {
	u := User{
		name: 'Peter'
	}
	if true {
	}
	// some comment after an if without else
	n := sizeof(User)
	// else
	// else {
	// }
	u2 := User{
		name: 'Henry' // comment after name
		// on the next line
		age: 42
		// after age line
		// after line2
	}
	_ := User{
		// Just a comment
	}
}
