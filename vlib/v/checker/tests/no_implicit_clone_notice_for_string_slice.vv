// Only array slices are implicitly cloned, so slicing a string must not be
// reported, neither from a mutable source nor into a mutable variable.
fn main() {
	mut text := 'hello world'
	text += '!'
	prefix := text[..5]
	mut suffix := text[6..]
	suffix += '?'
	println('${prefix} ${suffix}')
}
