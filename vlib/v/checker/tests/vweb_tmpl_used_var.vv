import vweb

struct App {}

pub fn (mut app App) index(mut c vweb.Context) vweb.Result {
    test := 'test'
    return $vweb.html()
}

fn main() {
	mut app := App{}
	vweb.run_app<App>(mut app, port: 8181)
}
