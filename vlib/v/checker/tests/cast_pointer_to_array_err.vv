fn main() {
	unsafe {
		values := [1, 2, 3]!
		p := &values[0]
		_ := []int(p)
		_ := [3]int(p)
	}
}

fn preserve_pointer_to_array_reinterpret_casts() {
	unsafe {
		dynamic := [1, 2, 3]
		_ := *&[]int(&dynamic)
		fixed := [1, 2, 3]!
		_ := *&[3]int(&fixed)
	}
}
